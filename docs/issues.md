- side-menu option "KI-Assistent" Should appear BELOW the sub-options for domain-verbund
- The application does not sufficiently guide the user through the process. The guidance could take the form of a small poup in the loer left corner telling the user what to enter in the currently active field, possibly also linking to additional information. The user should be able to disable the guide once he gains familiarity.
- it is absolutely unclear what a user needs to do in the tab "Anforderungen". Selecting a requirement opens a Form with two fields, the first for text to show before the requirement, thee second for text to show after the requiremt. What exactly does that mean?
- in Grundschutzcheck, saving of entered information fails.
- The KI tab requires a user to enter the requirement id, title and text manually??? At the moemnt that section is useless.. The tabs all only show the same form. If a requirement needs to be selected, why not have a dropdown allowing the user to select it? And the dfferent tabs might need different information, not all the same
- the main premise of the app is to GUIDE a user through the process not to leave him guessing. This premise is not kept at all.
- In Grundschutzcheck: The text of the requirement should be shown below the headline, so the user knows what this is about, as the headlines can appear multiple times in different context. The user must know at every point where he is, what is expected of him, and what to enter
